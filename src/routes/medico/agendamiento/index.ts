import { Router } from 'express';
// import { authenticate, authorize } from '../../../middleware/auth';
// import { UserRole } from '../../../types';

const router = Router();

// TODO: Implementar rutas de agendamiento médico
// router.get('/configuracion', authenticate, authorize(UserRole.MEDICO), ...);
// router.post('/crear-agenda', authenticate, authorize(UserRole.MEDICO), ...);
// router.get('/citas', authenticate, authorize(UserRole.MEDICO), ...);
// router.put('/citas/:id/confirmar', authenticate, authorize(UserRole.MEDICO), ...);

export default router;

