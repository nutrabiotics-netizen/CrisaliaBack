import { Router } from 'express';
// import { authenticate, authorize } from '../../../middleware/auth';
// import { UserRole } from '../../../types';

const router = Router();

// TODO: Implementar rutas de agendamiento paciente
// router.get('/medicos', authenticate, authorize(UserRole.PACIENTE), ...);
// router.post('/citas', authenticate, authorize(UserRole.PACIENTE), ...);
// router.put('/citas/:id/cancelar', authenticate, authorize(UserRole.PACIENTE), ...);

export default router;

